// constants/styles.ts
import { StyleSheet } from "react-native";

// Feste Styles die überall gelten sollen -gina
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#1C313B",
  },
});

export default styles;